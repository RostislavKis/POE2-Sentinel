Fix auto-restart using cmd.exe to fully detach new process

- Use cmd /c start to launch new exe in completely separate process tree
- Prevents Python DLL conflicts from inherited environment
- Added helpful message if auto-launch fails

Fix auto-updater dialog and update process

- Dialog now auto-sizes based on message content (fixes buttons not visible)
- Updater now shows progress window (visible PowerShell console)
- Added delays to ensure file handles are released before update
- Delete old exe before copying new one to avoid conflicts
- Better error messages and user feedback

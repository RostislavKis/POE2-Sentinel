Switch to single-file exe build for auto-updates

- PyInstaller now builds a self-contained single exe (62MB)
- No more _internal folder dependency
- Auto-updater can now replace the exe in-place
- Updated installer.iss to use new dist layout

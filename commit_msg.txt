Release v1.0.2

- Fixed update dialog buttons not visible (auto-size based on content)
- Fixed auto-updater Python DLL error (better cleanup and delays)
- Updater now shows progress window during update
